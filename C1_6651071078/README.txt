THE COZY BEAN - 3 PHIEN BAN

01_HTML_CSS_rieng:
- index.html
- css/style.css

02_HTML_CSS_chung:
- index.html (HTML + CSS chung)

Cả hai phiên bản đã đổi phần hình món sang ảnh trên web.
Vì ảnh được gọi bằng URL trực tiếp nên máy cần có Internet khi mở trang.

Lưu ý: một số món dùng ảnh minh họa gần với tên món nếu kết quả tìm kiếm không có ảnh trực tiếp phù hợp.
